# All Fire Services Claude Code Prompt Pack

Start by copying `00_MASTER_CLAUDE_CODE_PROMPT.md` into Claude Code or place this entire folder inside the repository, for example:

`refactor-plans/allfire-simple-refactor/`

Then tell Claude Code:

> Read every Markdown file in `refactor-plans/allfire-simple-refactor/` in filename order. Audit the existing codebase and implement the full refactor. Do not stop after planning. Run the production build and complete the acceptance checklist before reporting completion.

The files are intentionally separated by section so the implementation can be completed and reviewed one section at a time.
